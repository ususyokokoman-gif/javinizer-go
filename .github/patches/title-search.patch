diff --git a/internal/r18devdump/store.go b/internal/r18devdump/store.go
--- a/internal/r18devdump/store.go
+++ b/internal/r18devdump/store.go
@@ -199,17 +199,19 @@ func (s *Store) MatchByDisplayID(ctx context.Context, id string) ([]models.DumpM
 	return matches, nil
 }
 
-// LookupMovie resolves a display dvd_id to a fully-populated DumpMovie with all
-// related entities joined. This is the zero-HTTP fast path for the r18.dev
-// scraper: when it hits, no r18.dev API call is needed at all. Returns
-// models.ErrDumpMiss on a miss; any other wrapped error indicates a degraded
-// dump (corrupt file, schema drift, I/O failure) that the caller should log
-// before falling back to HTTP.
+// LookupMovie resolves a display dvd_id first, then falls back to an exact
+// title match against title_ja/title_en. Product-number lookup always has
+// priority so adding title search cannot change an existing dvd_id result.
+// English title matching is case-insensitive; Japanese title matching is exact.
+// This is the zero-HTTP fast path for the r18.dev scraper. Returns ErrDumpMiss
+// when neither lookup matches.
 func (s *Store) LookupMovie(ctx context.Context, dvdID string) (*models.DumpMovie, error) {
-	if s == nil || dvdID == "" {
+	query := strings.TrimSpace(dvdID)
+	if s == nil || query == "" {
 		return nil, models.ErrDumpMiss
 	}
-	norm := normalizeDVDID(dvdID)
+	norm := normalizeDVDID(query)
 	if norm == "" {
 		return nil, models.ErrDumpMiss
 	}
@@ -244,11 +246,35 @@ func (s *Store) LookupMovie(ctx context.Context, dvdID string) (*models.DumpMovi
 		&siteID, &serviceCode,
 	)
 	if errors.Is(err, sql.ErrNoRows) {
-		return nil, models.ErrDumpMiss
+		// The display-ID path missed. Fall back to an exact title search over
+		// the bilingual dump fields. Keep the query parameterized and select a
+		// deterministic row when duplicate titles exist.
+		err = s.db.QueryRowContext(ctx, `SELECT
+			content_id, dvd_id, title_en, title_ja, comment_en, comment_ja,
+			runtime_mins, release_date, sample_url,
+			maker_id, label_id, series_id,
+			jacket_full_url, jacket_thumb_url,
+			gallery_full_first, gallery_full_last,
+			gallery_thumb_first, gallery_thumb_last,
+			site_id, service_code
+			FROM videos
+			WHERE title_ja = ? OR title_en COLLATE NOCASE = ?
+			ORDER BY CASE WHEN title_ja = ? THEN 0 ELSE 1 END, content_id
+			LIMIT 1`, query, query, query,
+		).Scan(
+			&m.ContentID, &dvdIDCol, &titleEn, &titleJa, &commentEn, &commentJa,
+			&runtime, &releaseDate, &sampleURL,
+			&makerID, &labelID, &seriesID,
+			&jacketFull, &jacketThumb,
+			&galleryFirst, &galleryLast,
+			&thumbFirst, &thumbLast,
+			&siteID, &serviceCode,
+		)
+	}
+	if errors.Is(err, sql.ErrNoRows) {
+		return nil, models.ErrDumpMiss
 	}
 	if err != nil {
-		return nil, fmt.Errorf("dump lookup movie %q: %w", dvdID, err)
+		return nil, fmt.Errorf("dump lookup movie %q: %w", query, err)
 	}
 
 	m.DVDID = dvdIDCol.String
diff --git a/internal/r18devdump/import.go b/internal/r18devdump/import.go
--- a/internal/r18devdump/import.go
+++ b/internal/r18devdump/import.go
@@ -180,6 +180,8 @@ func Import(ctx context.Context, r io.Reader, path string, opts ImportOptions) (I
 		schema += s.create + ";"
 	}
 	schema += `CREATE INDEX idx_videos_dvd_id_norm ON videos(dvd_id_norm);
+		CREATE INDEX idx_videos_title_ja ON videos(title_ja);
+		CREATE INDEX idx_videos_title_en_nocase ON videos(title_en COLLATE NOCASE);
 		CREATE INDEX idx_video_actresses_cid ON video_actresses(content_id);
 		CREATE INDEX idx_video_categories_cid ON video_categories(content_id);
 		CREATE INDEX idx_video_directors_cid ON video_directors(content_id);`
